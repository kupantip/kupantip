services:
    backend:
        build:
            context: ./backend
            dockerfile: Dockerfile
        image: backend-kupantip
        container_name: backend-kupantip
        ports:
            - '${PORT:-8000}:8000'
