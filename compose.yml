services:
    backend:
        build:
            context: ./backend
            dockerfile: Dockerfile
        image: kupantip-backend
        container_name: kupantip-backend
        ports:
            - '${PORT:-8000}:8000'
        networks:
          - kupantip-net

networks:
  kupantip-net:
    external: true